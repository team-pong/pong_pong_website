const err0 = "non error";  // 에러 없음
const err1 = "already added data in table";  // 이미 데이터가 추가되어 있음
const err2 = "non user";  // 찾으려는 유저는 없음
const err3 = "no data found in table";  // 찾으려는 데이터가 없음
const err4 = "non channel";  // 찾으려는 채널이 없음
const err5 = "user is admin";  // 유저는 관리자임
const err6 = "wrong channel infomationr";  // 잘못된 채널 정보
const err7 = "no one in channel";  // 해당 채널에 아무도 없음
const err8 = "someone in channel";  // 해당 채널에 누군가 있음
const err9 = "user on another";  // 유저가 다른 채널에 있음
const err10 = "wrong game infomation";  // 잘못된 게임 정보


export {err0, err1, err2, err3, err4, err5, err6, err7, err8, err9, err10};